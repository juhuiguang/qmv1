1、每个页面访问时，都需要传递module_id（模块编码）与menu_id（菜单编码）参数。通过菜单访问时，菜单自动加上这两个参数。
所有页面功能中存在页面跳转的地方主义添加这两个参数。

2、用户菜单权限，如果菜单记录为all，表示该用户拥有全部菜单权限。

3、用户数据权限，在tb_user中，用户user_purview字段为all，表示拥有全局权限。

4、第一阶段，首页与各模块的首页，都为平铺导航页面。后期将替换成各主题界面。